---
title: The ReactJS form library
description: Createform is an open source project that allows you to create forms easily.
tags: react, form, useform, createform, use-form, hook
---

# Welcome to CreateForm

CreateForm is a powerful and flexible form library for ReactJS applications that simplifies form creation with minimal code and maximum performance.

## Why Choose CreateForm?


### 🚀 **High Performance**
- **Minimal re-renders**: Optimized with external store and debouncing
- **Uncontrolled by default**: Superior performance compared to controlled forms
- **Tiny bundle**: Only 3.3kB minified + gzipped (10.6kB minified)

### 🔧 **Two Powerful Approaches**
- **`createForm()`**: Create reusable custom hooks for complex form logic
- **`useForm()`**: Native HTML form handling with minimal setup

### 🛠 **Developer Experience**
- **Less code**: Minimal boilerplate compared to other form libraries
- **TypeScript support**: Full type safety out of the box
- **React focused**: Optimized specifically for React applications


## Quick Examples

### Using createForm()
```tsx
import { createForm } from "@createform/react";

const useLoginForm = createForm({
  initialValues: { email: "", password: "" },
  validationSchema: yup.object({
    email: yup.string().email().required(),
    password: yup.string().min(6).required(),
  }),
});

function LoginForm() {
  const { register, handleSubmit } = useLoginForm();
  
  const onSubmit = (data) => {
    console.log(data);
  };
  
  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input {...register("email")} />
      <input {...register("password")} />
      <button type="submit">Login</button>
    </form>
  );
}
```

### Using useForm()
```tsx
import { useForm } from "@createform/react";

function SimpleForm() {
  const { register } = useForm({
    initialValues: { name: "", email: "" },
    onSubmit: (data) => console.log(data),
  });

  return (
    <form {...register()}>
      <input name="name" placeholder="Name" />
      <input name="email" placeholder="Email" />
      <button type="submit">Submit</button>
    </form>
  );
}
```

## Key Features

- ✅ **Nested object support** with dot notation
- ✅ **Validation** with Yup, Zod, or custom validators
- ✅ **Multiple modes**: onChange, onBlur, onSubmit, debounce
- ✅ **External store management** with useSyncExternalStore
- ✅ **Async data loading** and form initialization
- ✅ **Custom components** integration
- ✅ **Comprehensive testing utilities**

## Getting Started

Ready to build better forms? Start with our [Quick Start Guide](/docs/quick-start) or explore the [API Reference](/docs/api-reference/create-form) for detailed documentation.

<div className="flex gap-4 mt-6">
  <a href="/docs/quick-start" className="bg-teal-500 text-white px-6 py-3 rounded-lg hover:bg-teal-600 transition-colors">
    Get Started →
  </a>
  <a href="/docs/api-reference/create-form" className="bg-gray-200 dark:bg-gray-800 text-gray-800 dark:text-gray-200 px-6 py-3 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-700 transition-colors">
    API Reference
  </a>
</div>